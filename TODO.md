# Payroll Calculation Fixes

## Pending Tasks

- [ ] Update NominaRow aggregation in `nomina.tsx` to calculate `hExtrasDiurnas`, `hExtrasNocturnas`, and `hDominicales`.
- [ ] Fix detail modal in `nomina.tsx` to use filtered dates (`fechaInicio` to `fechaFin`) instead of hardcoded dates.
- [ ] Test the fixes to ensure summary values match detail calculations and dates align.
